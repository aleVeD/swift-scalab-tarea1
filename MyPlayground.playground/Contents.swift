import UIKit

var str = "Hello, playground"
enum Estados {
    case Exitoso
    case NOEncontrado
    case Fallido
}
let current = Estados.NOEncontrado
func checkEstados(){
    switch current {
    case .Exitoso : break
    case .NOEncontrado : break
    default : break
    }
}

type(of: 1)
type(of:  "bhr")
sqrt(64)
type(of: 1) == String
2.words
let sfs : Float = Float.init().Type(8)
